apiVersion: networking.istio.io/v1beta1
kind: DestinationRule
metadata:
  name: credit-backend-dr
  namespace: credit-scoring
spec:
  host: credit-backend-svc
  subsets:
    - name: stable
      labels:
        version: stable
    - name: canary
      labels:
        version: canary
